window.onload = () => {
  let userInput;
  let msgPrompt = `Please enter a variable name.`;

  // Prompting the user to enter a name.
  // Capturing the name input.
  userInput = prompt(msgPrompt);

  // Using that input to declare a variable.
};
